# 下一轮网页端审阅提示词

请审阅这批 HoloCue Cloud Review 文件。所有 ZIP 均可独立解压，合并各包目录后，先阅读 `00_review_index.zip` 中的清单、状态和图片索引，再阅读本批 `FINAL_SUMMARY.md`、`COVERAGE.json`、`REVIEW_NOTES.md`、`RESOURCE_STOP.json` 及三个独立审查报告。用 `UPLOAD_INDEX.json` 和 `SHA256SUMS` 核对上传是否齐全。

本批源码摘要为 `37b81f92c4bb91b5ca68180be99b1f1810b40657842cf2d3145bd1eeb8d3f0fe`，实现提交截至 `ca213c5d6ea9bd3b9dc7f175cac7aefb7df22ff0`。源码、实际活动 GLB、固定 fixture 和三份新 `.blend` 随包。每项检查保存其自己的源码、资产和执行身份；519 项测试、三场景构建、真实模型专项流程及最终原生脚本属于不同明确记录的阶段，请保持这些区别。

API 因主机内存低于项目保留阈值被资源守卫停止。本批 blocks 完整通过 13 阶段；CNC 最新批次完成 9 个捕获阶段后中断，整场未完成。其余六场完整原生及 CLAMP、BLUE、BASKET、空接收孔、FLAG 的三组定向配对未运行。请检查已存在的原图、原始模型回复和日志，不能用云端包内结果或历史截图替代这些缺失结果。

重点审阅以下已有问题及证据：

- 工作区域标签在重复选择和窄窗口下的修复；blocks B 引线局部间隔仍为 P3 观察，原因未证实。
- CNC 接收位置图两端几何一致，但内孔不可见、提示上部有裁切；区分捕获程序断言和视觉可用性。
- 新材质分区与四份 GLB 是否符合实际几何语义；原有固定 fixture 字节是否保留。
- 审计相机稳定等待是否正确使用实际客户端及至少三个不同时间的导出；CNC 中断与真实资源停止记录是否一致。
- 当前资产、三份新 `.blend` 与九份历史构建来源之间的对应关系；tools 和辅助脚本的补充哈希及历史修订缺口。

只依据实际提供的文件给出问题、证据位置、严重程度和限定的后续任务。不要宣称十二场景整体原生验收完成、真实光学系统已标定或游戏级视觉质量已获认证。若给出新的修改包，请针对当前提交和实际文件生成，并保留用户修改及固定 fixture；资源停止后的实验恢复需要新的明确安排。
