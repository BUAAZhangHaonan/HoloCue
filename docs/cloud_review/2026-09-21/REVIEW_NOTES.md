# HoloCue Cloud Update 审阅说明

本批对应 `HoloCue_Cloud_Update_20260921.zip`，服务器为 4029，项目为 `/home/hdd3/zhanghaonan/projects/holocue`。请先读取 `FINAL_SUMMARY.md`、`COVERAGE.json` 和三个独立审查报告，再依据上传清单查看原始文件。每个 ZIP 可独立解压；完整审阅需要合并所有分包中的文件。

资源守卫已实际触发：2026-09-21 09:09:07 UTC，主机可用内存 50.027195 GiB，低于 50.309079 GiB 保留阈值，API 被守卫停止。按包内要求停止后续实验并交付已有资料。本批完整原生通过 blocks 一场（13 阶段）；CNC 最新批次完成 9 个捕获阶段后中断，整场未通过。其余六场完整原生和三组定向配对未运行。此前用户选择继续完整验收；最终停止由实际资源规则触发，不能描述为因额度主动缩减。

## 实现和证据身份

- 基准提交：`c4463b3a9842b3945ab7d1582ab8e49fcadca245`。
- 实现提交：`ca213c5d6ea9bd3b9dc7f175cac7aefb7df22ff0`。后续文档提交另由提交记录标识。
- 最终运行源码摘要：`37b81f92c4bb91b5ca68180be99b1f1810b40657842cf2d3145bd1eeb8d3f0fe`。
- 几何检查和三场景构建来自 `144eb217ca914243fd631bdff095a55ed4ecd949f6d6cec2bc27fa48012f488d` 阶段。此后改动涉及页面标签、跟随行为和验收脚本的相机同步；构建输入保持一致，原始运行摘要保留。
- 真实模型专项流程来自 `bd938aafb0b6e056bfecb53aa88ca189ae43d3724489194e34bfe53bffc3be24` 阶段。最终阶段对保存的模型回复进行只读一致性核查；不能将这次核查描述成重新运行模型流程。
- 最新完整 pytest 的 519 项、最终十二场景页面检查和 blocks_final 来自 `b2c379ba354ce95e6e48d5a9e73f2ff5bcfecb22acf823b785101b6df5ea2457` 阶段。此后仅修改原生审计脚本的相机稳定等待与失败诊断记录，应用源码及资产未变。后续原生流程的实际摘要见各自 `execution.json`。`COVERAGE.json` 在每项检查旁列出其执行身份。
- 原始摘要算法不纳入 `tools/` 与 `.work/`。`source_supplement.json` 补充实际验证工具及锁文件的哈希；回传清单另保存本批辅助脚本的字节哈希。较早失败运行使用过的辅助脚本修订并未全部在运行当时独立封存，当前辅助脚本不能作为这些旧修订的原始副本。

## 已合并范围

导入经校验的四份 GLB：engine_bay/workstation、shelf_picking/BLUE、shelf_picking/BASKET、drone_bench/BAY。其余 72 份活动场景 GLB 保持原字节，22 份原有固定 fixture 保持原字节；新增四份材质回归 GLB 及其清单。实际场景引用共 76 份 GLB，磁盘中另有历史文件。

新增成熟 glTF 验证器的严格格式检查和固定依赖锁文件；建模材质划分保持几何并集及外表面。页面增加工作区域标签、连接线和读取 Session 的状态说明。独立审查后修复窄窗口标签布局异常、跟随恢复时实际观察对象不同步、重复选择工作区域时标签未恢复三项问题。验收脚本等待真实客户端相机完成更新后执行聚焦检查，保留原有几何、渲染和模型参数。

三处文件读取、M 镜面固定环境方向及 CLAMP/L1/L2 的基础修改来自上一批已合并基准，本批予以保留。旧批运行记录和云端包内检查均单独归类。

## 资料解释与已知限制

- 自动几何结果覆盖 18 条有序动作、每条 41 个时刻、1536 个表面采样点、seed 4701 和 0.75 毫米阈值；不能外推为连续运动全部表面的碰撞证明。
- 三份新 `.blend` 有本批输入及输出哈希；另外九个场景沿用历史构建来源。当前原生捕获与静态场景构建分别记录。
- 窄窗口的主要对象处于画布内，但几何显示较小。独立视觉审查仍记录 blocks 的 B 标签连接线与文字之间存在局部间隔，严重程度 P3；可能与桌面遮挡有关，原因尚未确认。
- CNC 的 `step_01_endpoint_receiver` 两端图像几何一致，但低视角主要显示侧壁、没有显示内孔，提示上部超出画面。该捕获阶段的自动断言通过不能替代空孔可见性验收；定向空孔检查本批未运行。
- Viser 与 Blender 配对图保留同会话冻结时钟、实体位姿、版本和实际相机证据。效果是否清楚应结合原图判断，流程断言通过不等于游戏级视觉质量认证。
- 光学响应仍为声明过的解析 Gaussian 预览模型。未建立真实光学系统，也没有本批真实光学标定、用户研究或论文结论验证。
- 完整视频超过单包限制时，保留服务器原始视频并提供全时长审阅编码及原始文件哈希、实际转码命令、尺寸、帧数、帧率和时长核验。未裁剪操作流程。
- 旧 viewer 的原始执行记录保留原字节。`archive_relocation.json` 记录原路径、归档路径和哈希，避免将重启后的同名文件误认为早先运行文件。
- viewer 日志记录了两次客户端断开时的 poll 线程 `cannot schedule new futures after shutdown` 异常。独立源码审查已记录该清理竞态；本批没有扩大为服务生命周期重构，不能声明清理路径没有异常。
- PNG 分层渲染中间文件及 EXR 留在服务器；上传原始最终配对 PNG、消费帧、状态、相机、清单和日志。全部实际资产及选入的原生场景文件随包提供。

## 失败记录

导入程序在完成八份二进制文件复制后，因把活动资产数与磁盘历史文件总数混用而退出；后续独立核验按场景实际引用检查 76 份 GLB、四份改动、其余 72 份不变和 fixture 原字节。

早期页面捕获存在相机调整后尚未完成同步、隐藏节点更新尚未完成的问题；标签重选问题也曾产生真实失败图像。保留早期目录、退出码和主动终止记录；最终每场景通过状态由其报告条目和实际图像单独判定，不能由目录存在推断完成。

早期 blocks 原生流程在三个阶段后因真实相机聚焦检查失败而停止。修复相机等待后创建新会话，`blocks_final` 完成 13 阶段。旧失败记录完整保留。其他运行的失败与未运行条目由 `COVERAGE.json` 和最终总结如实列出。

CNC 首批在五个阶段后因相机瞬时匹配失效而停止：检查程序以旧特写相机计算期望焦距，按钮已按新工作区域相机正确聚焦。独立回算确认实际 3.427495883 米与新相机契约相符。稳定等待前版完成原暂停会话的 12 次真实焦点切换，状态完全不变；最终脚本进一步要求连续 0.75 秒稳定与至少三个不同时间的相机导出，后续流程单独记录。保留旧失败与前版专项检查的原始摘要。

## 独立审查入口

`code_review.md`、`visual_review.md`、`provenance_review.md` 分别来自 `/root/cloud_code_review`、`/root/cloud_visual_review`、`/root/cloud_provenance_review` 三个独立会话。各报告注明实际读取文件、图像和摘要。审查本身没有启动额外实验；未检查的范围不得引用这些报告作为通过证据。
