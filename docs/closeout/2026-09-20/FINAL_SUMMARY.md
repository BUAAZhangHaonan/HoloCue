# HoloCue 提前收尾总结

日期：2026-09-20。按用户最新要求控制额度，停止追加实验，收拢剩余三个开发子任务，保存全部代码、失败证据和未完成清单，分项提交并推送仓库。本次是**开发阶段提前收尾，不是十二场景最终验收通过**。

项目：4029 的 `/home/hdd3/zhanghaonan/projects/holocue`。仓库：`BUAAZhangHaonan/HoloCue`，分支 `master`，原始基准 `8ac969fc2adbd5095720f8b1c03c5683b4b1e3b3`。

## 已交付的实现

- 接入真实本地 Qwen3.5-4B，统一有序任务、持久状态、暂停、临时检查、明确完成和恢复流程；保留原任务身份、动作参数、接收对象及重复对象的独立步骤。
- 使用同一场景契约处理模型坐标、动作、接合、检查面、相机与完成后的实体位姿。统一十二场景资产生成、Viser 显示与 Blender 同会话桥接，迁移旧入口并保留有效测试。
- 实现焦点、亮度、N、sigma 对三维提示的解析响应及参数来源；保持 `analytic-gaussian-preview-v1`，没有换成任意全屏模糊。
- 保存真实双客户端、取消/快速指令、API 重启、过期状态和模型连接失败等检查，以及资源守卫、进程归属和官方 Viser 接口修正。
- 收拢地面支撑、接口开口、阀座、输液旋塞、安装座、CLAMP 和镜片支柱等开发修复；增加不可变基线资产与几何回归。

**提交中的最新构建源码已经包含最后一轮修复，但正式场景 GLB 与 `.blend` 仍是此前实际执行过的版本。最新源码尚未完成统一资产重建与全量回归。不能把现有旧资产、旧截图或此前通过记录称为最终源码的通过结果。**

此前运行版本的源摘要为 `03bb20badda47fd4a1be00df8418e99cc7c440e99a7e2b73696af4b4874c9159`，对应提交链截至 `3b8a7e7cc42afc919ee6b842e778123350e7957f` 所保存的运行代码和资产。最新 `src/holocue/modeling.py` SHA256 为 `0a7bdcb1e06ede04fb4fcd4689a25aa5cce9a4e347fe68420dffd0c9130837aa`。该单文件摘要不是全仓验收摘要。

## 三个开发子任务的收尾

| 子任务 | 已完成 | 未完成 |
|---|---|---|
| CLAMP 与软管/金属座 | 私有候选15项通过；原协议 CLAMP/PLUG 命中94/0降为0/0；局部源码、新测试和5份固定fixture已合并 | 78项合并回归在生成前被资源守卫终止；新GLB为0，pytest未执行，九场景对比未完成；没有候选原生视觉复验 |
| L1/L2 镜片支柱 | 私有候选11项通过；缩短支柱并加入真实盲座；原可见遮挡射线改为先命中镜片；保留CLAMP修改完成局部合并 | 89项联合回归未运行；最终统一资产未生成；原生复验未运行 |
| M 镜面偏黑诊断 | 实际客户端确认HDR加载、纹理和材质正常；64个正面三角的反射方向均落在studio暗幕；原会话状态未变，诊断已封存 | 建议的官方环境方向调整未实施；没有受控光照对照，也没有修复后的视觉通过结论 |

三个开发记录分别见 `CLAMP_INTEGRATION.md`、`LENS_MERGE.md`、`MIRROR_DIAGNOSIS.md`。它们是本次三个现有开发代理的收尾材料，**不是原目标要求的三个全新独立最终验收报告**。

## 实际验证与证据边界

以下运行路径位于旧验收目录 `runs/simulation/acceptance_20260919_1706/`。

| 已实际执行的检查 | 结果与范围 |
|---|---|
| `repair_pytest_r3` | 旧运行版本319项通过，实际退出0；不是最后合并版本的全量测试 |
| `live_r22` | 真实模型十二场景、30有序步骤通过；保存原始请求、回复和任务状态 |
| `browser_r20` | 真实十二场景UI通过，0页面错误；主线程查看图像与每场33个录像样本 |
| `response_r21` | 真实焦点/亮度与N、sigma响应检查通过；主线程查看3张PNG与33个录像样本 |
| `resilience_engine_repair_r1` | 9项故障与隔离检查通过；包含两个客户端、API重启和模型连接失败后的显式恢复；过期显示包括实际快照回放，不宣称网络包注入 |
| `ordered_geometry_candidate_audit_r1` | 扩展到18个有序运动后，旧资产真实失败：B41、STEM82、GUARD41、CLAMP94、M41；原失败报告保留 |
| 支撑/阀座/安装座的分阶段集成 | 48项、继而63项实际通过；分别对应各自开发快照；不能推导最终89项或全套测试通过 |
| CLAMP / lens私有回归 | 分别15项、11项通过，保存负向旧基线与失败历史；完整动作采样协议未降低 |
| 最后轻量收尾 | 83个Python文件语法检查、31个JSON解析、Git空白检查通过；55个待同步文件远端SHA逐项一致；未追加模型、浏览器、资产生成或原生渲染 |

几何运动采用1536表面点、seed4701、41个时间点、0.75毫米阈值，并纳入此前完成的对象位姿。零采样命中不等于连续运动无碰撞证明。录像检查使用明确时间点的抽样图，不声称连续观看全部录像或已生成Blender动画。

## 十二场景原生桥接状态

`bridge_r16_all/report.json` 实际列出10场完整、1场部分完成、1场失败。完整的10场合计162阶段、54操作对象、16结构检查对象，主线程已看完其配对PNG和每场33个录像样本。此处“完整”指该次执行覆盖完整，仍有后续发现的视觉问题，不能理解为最终质量通过。

| 场景 | 已完成原生阶段 | 本批状态 |
|---|---:|---|
| engine_bay | 17 | 流程与对象捕获完整；后续CLAMP/支撑修复待复验 |
| cnc_toolchange | 17 | 流程与对象捕获完整；后续地面支撑待复验 |
| connector | 13 | 流程与对象捕获完整 |
| control_panel | 11 | 流程与对象捕获完整；后续安装座/支撑待复验 |
| blocks | 12 | 流程与对象捕获完整 |
| dig_site | 17 | 流程与对象捕获完整；FLAG接触修复待复验 |
| dive_fillstation | 22 | 流程与对象捕获完整；阀座/支撑/观察方向待复验 |
| drone_bench | 19 | 流程与对象捕获完整；GUARD座待复验 |
| infusion_ward | 18 | 流程与对象捕获完整；旋塞与支撑修复待复验 |
| optical_bench | 16 | 流程与对象捕获完整；镜片穿插和暗镜面尚未最终复验 |
| server_rack | 5 | 原定NODE3检查渲染被守卫终止；final及逐对象捕获未完成 |
| shelf_picking | 0 | 模型连接失败，未开始该批原生捕获 |

原预期188阶段没有完成。新审计源码已补充11个接收对象终点视角，未来预期199阶段，但没有执行这次扩展原生批次。用于188阶段完整成功的封存脚本未运行，不能据此宣称完整封存成功。

原报告SHA256：`ed15ca9bba70beb376d99e3f0b5b4d01d61ebdedbcf5aa083d5de3a30d4878c7`。由于外层守卫也终止了驱动进程，`bridge_r16_all_execution/execution.json` 没有形成；保留实际场景报告、命令日志、阶段退出码和资源记录，不补造外层退出记录。

## 资源终止与当前服务

2026-09-20约20:47–20:49（北京时间），主机可用内存触及实际守卫保留阈值50.309079GiB。server检查采样到50.218472GiB时被停止，其自身RSS约0.461GiB；CLAMP集成采样到50.031826GiB时被停止，其自身RSS约0.497GiB。不能把它们说成应用自身超过RSS限制，也没有据此推断其他用户任务的原因。

守卫随后停止了本任务的模型、API、Viewer与原生批次。收尾核查记录的PID/create_time身份均已结束，8750/8780没有监听。没有重启实验、降低参数或停止其他用户服务。

实际使用物理GPU1运行Qwen3.5-4B，GPU2运行原生渲染；应用CPU进程保持空CUDA。模型使用独立环境，vLLM0.29.0、max_model_len8192、max_num_seqs1、显存比例0.70、禁用thinking、原生sampler；应用Python3.12.7、Viser1.1.1及已记录客户端补丁，Blender3.1.2。模型请求max_tokens1600、temperature0.2、top_p0.8。未为收尾降低这些设置。

此前资源/临时目录及失败历史均保留，包括历史VTK/GPU0、系统临时目录、早期原生批次和校验工具失败的记录；本报告不把最终守卫正常终止推导成整个历史过程从无违规。

## 未完成清单

1. 在最新合并源码上统一生成十二场景GLB及`.blend`，完成89项联合回归、完整pytest、场景/渲染检查和18条有序运动审计。
2. 对CLAMP、L1/L2、旋塞、安装座、地面支撑、FLAG及新增观察视角做真实原生复验；镜面环境调整仍只有建议。
3. 完成server/shelf及最终源码下需要重跑的模型、UI、响应、故障与同会话原生验收；补齐11个接收视角和全结构支撑视角。
4. 在同一最终源码上建立三个全新独立最终审查会话并完成视觉、状态、架构安全审查。**本项未执行。**

以上按用户要求停止追加，不在本次收尾后自动继续。

## 文件、提交与后续复现入口

最终文档与轻量核查另存于服务器 `runs/simulation/closeout_20260920/`，并将可审阅的小体积报告纳入 `docs/closeout/2026-09-20/`。原始运行证据约70GB保留在原验收目录；私有候选、缓存和开发中间物保留在项目`.work/`。模型、虚拟环境、认证资料、缓存和原始运行数据不进入Git。

代码、契约、回归fixture、正式GLB、场景设计文档均已分功能提交。12个原生`.blend`使用Git LFS；它们是旧运行版本的真实资产，非最后源码新生成结果。固定fixture禁止换行转换，保持测试所核验的原始字节。最终提交/远端推送身份在发布核对记录与本任务最终回复中列出，避免在文档自身嵌入自引用提交摘要。

后续若另行授权继续，先检查资源并读取本报告，再读取`README.md`的环境变量设置；运行入口为`scripts/ops/serve_model.sh`、`scripts/run_simulation.sh api`和`scripts/run_simulation.sh viewer`，生成入口为`scripts/scenes/build_all.sh`。正式检查包括`pytest`、`scripts.tests.live_twelve_scenes`、`scripts.tests.audit_geometry`、`scripts.tests.audit_viewer_live`、`scripts.tests.audit_response_live`、`scripts.tests.audit_resilience_live`和`scripts.tests.audit_bridge_live`。必须设置项目内部缓存并使用现行资源守卫，不能在没有重新验证资源时直接照搬旧PID或启动任务。

SSH访问仍可采用`ssh -L 8780:127.0.0.1:8780 -L 8750:127.0.0.1:8750 4029`；当前服务已停，因此该隧道本身不会使网页恢复。
