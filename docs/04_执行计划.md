# 十二场景仿真执行计划

## 目标与边界

在 4029 的 `/home/hdd3/zhanghaonan/projects/holocue` 完成真实 Qwen3.5-4B、持久任务状态、Viser 与 Blender 的十二场景系统。操作从任务解释、显示、暂停、临时检查、明确完成到恢复和最终完成；原任务标识、动作参数、接收对象与有序步骤必须保持一致。

## 环境和实现

先读取当前代码、用户修改、端口与进程归属、现场内存规则及物理 GPU 1、2 的占用。复用已有模型环境和 Blender；应用使用 `.venv-simulation`，通过项目依赖安装 dev 和 simulation 组件。所有临时目录与缓存按照 README 设置到项目内部。资源守卫采用现场与项目约束中更严格的值。

场景配置、实际模型、动作轴、接合位置和检查面使用共同契约。统一构建入口为 `scripts/scenes/build_all.sh`。资产调整保留已有有效细节、许可与来源；源代码编辑使用文件编辑工具。应用从 `scripts/run_simulation.sh api|viewer` 启动，绑定环回地址并通过 SSH 隧道访问。

## 验证顺序

1. 完整执行有效自动测试、场景检查、几何检查与渲染检查。
2. 执行 `scripts/tests/live_twelve_scenes.py`，保存全部真实模型输入输出与状态。核对有序步骤、常驻目标、挂起记录、恢复标识与完成位姿。
3. 执行 `python -m scripts.tests.audit_viewer_live`，实际操作网页，保存两种窗口尺寸、特写、检查面、焦点与亮度变化证据。
4. 对十二场景执行同会话 Blender 桥接，核对对象、版本、相机、任务时间与提示响应。视频或帧序列覆盖起点、动作过程、终点等待、临时检查与恢复。
5. 使用两个独立浏览器上下文核对会话、场景与相机隔离；检查快速指令、模型请求中断、API 重启、过期显示包、模型断连及恢复后的显式继续。

详细检查与证据要求以 [验证标准](VALIDATION.md) 为准。接口以 [接口说明](API.md) 和当前代码为准。

## 独立审查与收官

三个独立 SubAgent 分别进行场景视觉、状态任务、架构与运行安全审查。它们直接读取同一份最终代码与原始证据，记录真实会话标识、实际命令和文件摘要。问题修复后重新执行受影响的检查；没有实际执行的项目明确列为待执行或阻塞。

新的验收目录保留模型与引擎、完整依赖版本、GPU 和内存记录、场景结果、执行及访问命令、图像视频、原始审查输出和剩余问题。最终报告确认完成后只停止本次创建并记录的服务。历史证据不覆盖、不改写。
