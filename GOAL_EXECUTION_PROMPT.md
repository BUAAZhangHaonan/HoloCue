# /goal HoloCue 第一阶段完整执行任务

你是本项目的实现负责人。把本文件作为完成目标，持续执行到功能与证据满足验收，或遇到真实权限、资源、依赖阻塞后明确报告。先阅读 AGENTS.md、README.md，以及 docs 中的三份 Markdown 主文档和执行计划。保留现有可用实现，按需修复，不要整套推倒重写。

## 目标与目录

在 4029 的 `/home/hdd3/zhanghaonan/projects/holocue` 建立可运行的 HoloCue 第一版。通过本地 Qwen3.5-4B、严格结构化语义输出、持久任务状态、Blender 和 Viser，实现三场景的任务提示、动态打断、参数保留与恢复。把真正的光学端留在稳定接口之后。阶段一只验收交互、参数和可视化；光学数据由合作者交付。

如果从 ZIP 开始，先检查压缩路径、文件清单和 SHA256，拒绝绝对路径与越界路径。压缩包只有 holocue 这个项目根目录。项目目录已有 Git 时保留 .git 和未提交修改，先对比再复制，不覆盖用户已有文件。压缩包可能放在当前目录、下载目录或用户指定目录，先查找，不猜造文件已存在。

## 安全前置检查

只读盘点 4029，保存硬件、磁盘、内存、环境、Blender 版本及物理 GPU 1、2 的 UUID 和占用。只有 GPU 1、2 可分配计算，禁止其他卡。Blender 与 Viser 默认 CPU。项目内建隔离环境，禁止 sudo、系统升级、修改无关项目或关闭其他人的进程。

读取服务器已有内存红线，与 docs/07 默认规则取更严格者。运行前检查、运行中监控、越线只终止本项目进程组。不得为通过验收放宽红线或借用其他 GPU。

## 实施顺序

1. 在 .venv 安装应用依赖，完成核心测试，检查模型 Schema、状态迁移和异步版本约束。所有修改留下理由和测试。
2. 检查九组 GLB/OBJ 固定资产与三个场景 JSON。复用已有 Blender，生成三个真实 .blend 文件和渲染图。检查对象标识、米制尺寸、朝向和参考锚点。
3. 先用显式 replay 联调网页与 Blender 桥接，保持 replay 标签。回放只用于软件检查，不作为最终模型验收。
4. 在 4029 及已配置连接的 4028 `/home/g203-4028/Models` 中只读查找 Qwen3.5-4B/9B。优先使用完整同名 checkpoint；需要复制时只复制到本项目模型目录。没有可用副本才显式下载官方模型，记录 revision 和文件检查。不要修改源文件。
5. 首先部署 4B，独立 .venv-model，优先 vLLM，SGLang 可作为明确选择的替代配置。按官方现行说明核对兼容版本和启动参数，记录准确版本。先使用 8192 上下文、单并发、非思考模式、0.70 显存比例。4B/9B、dtype、引擎切换均作为显式实验配置，不能隐藏失败。
6. 启动 live Agent API 与 Viser，运行 scripts/live_checks/live_smoke.py 和 scripts/live_checks/live_interrupt.py。核心必须包括 B 转 30 度、临时查看 C 背面、确认 C 完成、恢复 B 且角度和 task_id 不变。用户打断后旧动作及时冻结，迟到模型响应失效。
7. 将同一会话接到 Blender 实时桥接。用带视觉能力的 SubAgent 查看真实 Viser 截图与 Blender 渲染，改正模型尺寸、遮挡、提示朝向、文字和暂停行为。截图必须来自实际运行，不用包内概念图替代。
8. 在三场景完成自然语言与按钮交互，测试未知对象、缺少角度、无效动作、模型断连、快速连发、服务重启恢复。失败需要在界面和日志可见。模型能力失败也记录，不用规则补正确答案。
9. 测量模型首个正确提示延迟、实际网页更新、内存与显存峰值，保存样本数与原始记录。页面30Hz目标不等于真实测得30FPS，更不等于CGH帧率。保留有限大小的视频和截图，控制文件体积。
10. 导出 DisplayPacket、JSON Schema、API 示例、resolved requirements、最终启动与关闭脚本。内网光学适配器保持 version、epoch、标定信息与单位明确。当前相对 σ 必须保留相对标签。

## 三次独立 SubAgent 校验是完成条件

完成实现并修复已知问题后，启动至少三个独立 SubAgent 会话。每位都从最终文件独立阅读，不沿用主 Agent 的“已通过”结论。

A 运行安全审查。核验物理 GPU 授权、UUID 映射、模型与引擎配置、主机/显存峰值、进程停止范围、模型失败可见性。检查现场日志与进程证据。

B 视觉与交互审查。必须具备并使用视觉能力，实际看三个 Viser 场景、三个 Blender 场景及中断恢复视频或帧序列。操作或复核相同会话的联动，检查箭头方向、动作终点、暂停、恢复、对象标签和 live 标记。

C 架构与复现审查。独立按 README 启动或检查重启流程，核验严格语义协议、预算守恒、σ 来源、数据单位、异步取消/旧响应、显式任务完成和未来适配器。重跑测试，核对交付目录与证据是否可复现。

每位保存一份 JSON 与一份易读报告。JSON 参考 verification/REVIEW_TEMPLATE.json，scope 分别为 runtime_safety、interaction_visual、architecture_reproducibility。independent_session_id 必须来自真实独立会话，source_sha256 使用 scripts/release/check_review_gate.py --digest；evidence 的路径与 SHA256 指向真实文件，视觉审查写 visually_inspected=true。

审查发现问题先修复，再对最终修改重跑相关检查。收官三份报告必须对应同一个最终代码摘要，至少三份 passed 且无未解决关键问题。执行 scripts/release/check_review_gate.py。该门只检查记录，不能替代真实复核。没有 SubAgent 工具或视觉能力时明确报告阻塞，不得自写三份报告伪装通过。

## 不应扩展的工作

本阶段不部署真实 SLM、不开展人眼光学实验、不训练新模型、不建立语音或眼动硬件、不增加多智能体辩论、不优化通用全场景CGH。授权的核心是现有代码的正确完成、真实本地模型和两个可视化环境的闭环。

## 最终答复必须包含

项目与服务实际路径，模型名称与文件来源，真实引擎版本与启动命令，物理 GPU/UUID，客户端 SSH 隧道及网页访问地址，三场景操作示例，测试与真实模型结果，Blender/Viser证据位置，内存日志，三份独立 SubAgent 审查位置，剩余问题和停止原因（如有）。最后给出重启和安全停止方式。

所有结论以真实执行为准。不把代码已经存在等同于依赖已装好，不把 replay 通过等同于 LLM 正确，不把焦深示意图等同于光学实验。
