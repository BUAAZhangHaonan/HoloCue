# `<scene_id>` 场景设计（as-built）

- 设计来源：scene_agent 提案 vN（三轮独立审查全 pass；报告见 `../reviews/`）。
- 世界 / 视点与深度表 / 任务脚本 / 交互对象表 / 环境 / 四判据论证：按 proposal_template 结构填写，
  数值以建模定稿后的 `../scene.json` 与 `runs/scene_v2/build_<id>.log` 实测为准。
